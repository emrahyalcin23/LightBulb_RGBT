namespace LightBulb.Models;

/// <summary>Which channel's curve is currently selected in the calibration graph editor.</summary>
public enum CalibrationChannel
{
    Brightness,
    R,
    G,
    B,
    L,
}
